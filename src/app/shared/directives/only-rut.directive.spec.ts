import { OnlyRutDirective } from './only-rut.directive';

describe('OnlyRutDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyRutDirective();
    expect(directive).toBeTruthy();
  });
});
