export interface MinuteData {
  id: number;
  label: string;
  transform: string;
}

export interface HourData {
  id: number;
  label: string;
  transform: string;
}

export interface PeriodData {
  id: number;
  label: string;
}
